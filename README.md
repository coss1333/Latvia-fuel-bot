# Latvia Fuel Prices Telegram Bot

Бот для получения **топ-20 самых дешёвых АЗС в Латвии** по выбранному виду топлива (A95, A98, Diesel, LPG) по запросу в Telegram.

## Что умеет
- Команда `/top <вид_топлива> [N]` — вернёт топ-N АЗС по цене на момент запроса (по умолчанию N=20).
- Команда `/fuels` — список поддерживаемых видов топлива.
- Источники данных: 
  - 🗺️ Сообщественный агрегатор Waze (через страницу **gas.didnt.work**) — покрывает множество АЗС в Латвии и обновляется примерно каждые 5 часов.
  - Официальные страницы сетей: **Circle K**, **Neste**, **Virši**, **Viada** (разделы с \"zemākās cenas\" / \"lowest prices\").  
- На выходе: название АЗС, адрес, цена, источник, (иногда) отметка даты/времени, если есть у источника.

> ⚠️ Ограничения: в Латвии нет одного официального публичного API со **всеми** станциями и их ценами в реальном времени. Поэтому бот опирается на открытые страницы и сообщественный источник. Результаты носят информативный характер и могут отличаться от фактических цен на стеле.

## Быстрый старт

1. Создайте бота у @BotFather и получите `TELEGRAM_BOT_TOKEN`.
2. Скачайте этот архив и распакуйте.
3. Создайте файл `.env` по образцу `.env.example` и впишите токен.
4. Установите Python 3.10+
5. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
6. Запустите:
   ```bash
   python main.py
   ```

## Команды

- `/start` / `/help` — справка
- `/fuels` — поддерживаемые типы топлива
- `/top a95` — топ-20 по A95
- `/top diesel 10` — топ-10 по дизелю
- `/top lpg` — автогаз

## Как это работает

- `fuel_sources.py` — сборщик из 5 источников (gas.didnt.work, Circle K, Neste, Virši, Viada). Парсит страницы и формирует список станций с ценами.
- `parser_utils.py` — утилиты форматирования.
- `main.py` — Telegram-бот на `aiogram`.

Во время ответа бот объединяет и дедуплицирует станции (по имени+адресу) и выбирает минимальную цену по каждому виду топлива, если один и тот же адрес встречается у нескольких источников.

## Доработки (опционально)

- Добавить источники: Latvijas Nafta, Kool, Astarte (если появятся публичные страницы с ценами).
- Сохранение кэша в SQLite с TTL, чтобы уменьшить нагрузку на источники.
- Geofilter: `/near <город>` — сначала отфильтровывать ближайшие к заданному городу.
- Вывод в CSV по команде `/export`.

## Дисклеймер
Этот бот использует открытые страницы, которые могут меняться. Если парсинг где-то сломается, обновите регулярные выражения в `fuel_sources.py`.
Проверяйте актуальные цены на стеле АЗС.
